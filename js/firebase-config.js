// Firebase settings: Google sign-in for the admin page, and where published
// site data is kept. Replace null with the firebaseConfig object from the
// Firebase console (Project settings > Your apps). These values are meant to
// be public; Firebase's security rules decide who can publish.
const FIREBASE_CONFIG = null;
